# Expose pbcopy/pbpaste/open/noti to remote host
RemoteForward 2224 127.0.0.1:2224  # pbcopy
RemoteForward 2225 127.0.0.1:2225  # pbpaste
RemoteForward 2226 127.0.0.1:2226  # open
RemoteForward 2227 127.0.0.1:2227  # noti

# Fix pbcopy/pbpaste doesn't work on hosts that have host key changed
StrictHostKeyChecking no
UserKnownHostsFile /dev/null
LogLevel QUIET

TCPKeepAlive no
ServerAliveInterval 5
ServerAliveCountMax 720

ControlPath    ~/.ssh/control/%C
ControlMaster  auto
ControlPersist 10d

# Speed up ssh connection
AddressFamily inet
VerifyHostKeyDNS no

# Turn on gzip compression
Compression yes

Include conf.d/*
