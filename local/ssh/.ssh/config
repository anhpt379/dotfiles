# Expose open/noti to remote host
RemoteForward 2226 127.0.0.1:2226  # open
RemoteForward 2227 127.0.0.1:2227  # noti

# Fix port forwarding doesn't work on hosts that have host key changed
# StrictHostKeyChecking no
# UserKnownHostsFile /dev/null
LogLevel QUIET

TCPKeepAlive no
ServerAliveInterval 1
ServerAliveCountMax 5

# ControlPath    ~/.ssh/control/%C
# ControlMaster  auto
# ControlPersist 10d

# Speed up ssh connection
AddressFamily inet
VerifyHostKeyDNS no

# Turn on gzip compression
Compression yes

Include conf.d/*
