#!/bin/bash
# https://superuser.com/a/1667623

if [ "$#" -ne 0 ]; then
  /usr/bin/clear "$@"
  exit
fi

h="$(tput lines 2>/dev/null)"
if [ "$?" -eq 0 ]; then
  until [ "$h" -le 1 ]; do
      printf '\n'
      h=$((h-1))
  done
fi

/usr/bin/clear -x
