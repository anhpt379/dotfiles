#!/usr/bin/env python3
# vim:sw=4

import sys

text = sys.stdin.read()
stripped_text = text.strip()
if stripped_text.count("\n") < 1:
    for char in ['"', "'", "`"]:
        if text[0] == char and text[-1] == char:
            text = text[1:-1]
            break
print(text, end="")
